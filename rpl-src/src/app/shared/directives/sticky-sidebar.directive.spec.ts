import { StickySidebarDirective } from './sticky-sidebar.directive';

describe('StickySidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new StickySidebarDirective();
    expect(directive).toBeTruthy();
  });
});
