import { UrlFragmentScrollerDirective } from './url-fragment-scroller.directive';

describe('UrlFragmentScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlFragmentScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
