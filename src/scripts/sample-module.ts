/**
 * A sample class with a single method that logs a message.
 */
export default class Sample {
  /**
   * Writes a message to the console.
   * @param message {string} The message to write.
   */
  static writeConsoleMessage(message: string): void {
    console.log(message);
  }
}
